import { DarknessFilterPipe } from './darkness-filter.pipe';

describe('DarknessFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DarknessFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
